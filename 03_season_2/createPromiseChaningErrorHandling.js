// Creating our own promise

const cart = ['shoes', 'pants', 'kurta'];

const createdOrder = createOrder(cart);

createdOrder.then( () => proceedToPayment())

///////////////////////////////////////

function createOrder(cart) {
    
}